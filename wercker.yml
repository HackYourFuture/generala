box: node

build:
  steps:
    - npm-install
    - bower-install

deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        basedir: client
        branch: master
